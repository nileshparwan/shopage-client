import { css } from 'styled-components';

export const layoutStyles = css`
  body {
    margin: 0;
    padding: 0;
  }
  .container {
    max-width: 1100px;
    margin: 0 auto;
    width: 100%;
  }
`;

export default layoutStyles;
